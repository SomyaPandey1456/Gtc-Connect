package com.example.gtcconnect;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class Core_team extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Set the layout file
        setContentView(R.layout.core_team);

        // You can initialize UI components here if needed
        // For example:
        // ImageView presidentImage = findViewById(R.id.president_image);
        // TextView presidentName = findViewById(R.id.president_name);

        // You can set data or add listeners if required:
        // presidentName.setText("Anirban Sarkar");
    }
}
