package com.example.gtcconnect;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class CsDivisional extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.cs_divisional); // Ensure this matches your XML file name

        // You can add additional logic here if needed, e.g., binding data dynamically
    }
}
